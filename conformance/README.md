# Conformance data

This directory will contain language-neutral deterministic fixtures and vectors
used to compare the independent Python and C implementations.

The initial format will be defined during pre-0.1 work before MAX31865 driver
implementation is considered stable.
