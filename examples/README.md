# Examples

Examples will begin with MAX31865 acquisition on Raspberry Pi and integration
with `rtd-sensor` while preserving the package boundary:

`physical RTD -> rtd-acquire -> resistance -> rtd-sensor`
